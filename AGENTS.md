# Automated Checks

After each code change, run the following commands to ensure code quality:
- `cargo test` - Run all tests
- `cargo fmt` - Format code according to style guidelines
- `cargo clippy` - Lint for potential issues